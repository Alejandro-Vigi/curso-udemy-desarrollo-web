// Objeto Math para numeros

let resultado;

resultado = Math.PI
resultado = Math.round(2.4); // 2 Redondea
resultado = Math.round(2.5); // 3  Redondea
resultado = Math.ceil(2.1);  // Siempre redondea hacia arriba
resultado = Math.floor(2.9);  // Siempre redondea hacia abajo
resultado = Math.sqrt(144); // Raiz
resultado = Math.abs(-200); // Absoluto
resultado = Math.min(3, 5, 1, 8, 2); // Numero minimo
resultado = Math.max(3, 5, 1, 8, 2); // Numero maximo
resultado = Math.random(); // Aleatorio
resultado = Math.floor(Math.random() * 30);

console.log(resultado);